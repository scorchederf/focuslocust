---
parsed_by: focuslocust
source: mitre
type: technique
aliases:
    - T1059.013
tags:
    - attack/domain/enterprise_attack
    - attack/mitigated
    - attack/subtechnique
    - attack/tactic/execution
    - attack/type/subtechnique
    - platform/containers
mitre-attack: kb/mitre/attack/techniques/T1059.013-container-cli-api
tactic:
    - Execution
platforms:
    - Containers
permissions required:
    - none
---

## Description

Adversaries may abuse built-in CLI tools or API calls to execute malicious commands in containerized environments.<br><br>The Docker CLI is used for managing containers via an exposed API point from the `dockerd` daemon. Some common examples of Docker CLI include Docker Desktop CLI and Docker Compose, but users are also able to use SDKs to interact with the API. For example, Docker SDK for Python can be used to run commands within a Python application.[^1] <br><br>Adversaries may leverage the Docker CLI, API, or SDK to pull or build Docker images (i.e., [[kb/mitre/attack/techniques/T1105-ingress-tool-transfer|Ingress Tool Transfer]], [[kb/mitre/attack/techniques/T1612-build-image-on-host|Build Image on Host]]), run containers (i.e., [[kb/mitre/attack/techniques/T1610-deploy-container|Deploy Container]]), or execute commands inside running containers (i.e., [[kb/mitre/attack/techniques/T1609-container-administration-command|Container Administration Command]]). In some cases, threat actors may pull legitimate images that include scripts or tools that they can leverage - for example, using an image that includes the `curl` command to download payloads.[^3]  Adversaries may also utilize `docker inspect` and `docker ps` to scan for cloud environment variables and other running containers (i.e., [[kb/mitre/attack/techniques/T1613-container-and-resource-discovery|Container and Resource Discovery]]).[^2] [^4] <br><br>Kubernetes is responsible for the management and orchestration of containers across clusters. The Kubernetes control plane, which manages the state of the cluster and is responsible for scheduling, communication, and resource monitoring, can be invoked directly via the API or indirectly via CLI tools such as `kubectl`. It may also be accessed within client libraries such as Go or Python. By utilizing the API, administrators can interact with resources within the cluster such as listing or creating pods, which is a group of one or more containers. Adversaries call the API server via `curl` or other tools, allowing them to obtain further information about the environment such as pods, deployments, daemonsets, namespaces, or sysvars.[^4]  They may also run various commands regarding resource management.

## Mitigations
| ID | Name | Description |
| --- | --- | --- |
| [[kb/mitre/attack/mitigations/M1026-privileged-account-management\|M1026]] | Privileged Account Management | Restrict permissions on API access. RBAC in Kubernetes involve permissions that are additive, meaning there are no explicit "deny" rules. These permissions can be defined within a particular namespace or within cluster-scoped resources. Securing the Docker daemon can be done by using SSH or TLS with certificate authorization. Container management tools such as Docker and Podman may offer ways to run containers as rootless, which prevents them from running with privileged permissions. |
| [[kb/mitre/attack/mitigations/M1038-execution-prevention\|M1038]] | Execution Prevention | Deny scripting where appropriate. Tools such as Python or Go can utilize Kubernetes and Docker within a client library and execute commands within their application. |

 [^1]: [Docker Desktop CLI](https://docs.docker.com/desktop/features/desktop-cli/)
 [^2]: [Cisco Talos Blog](https://blog.talosintelligence.com/teamtnt-targeting-aws-alibaba-2/)
 [^3]: [Intezer](https://intezer.com/blog/watch-your-containers-doki-infecting-docker-servers-in-the-cloud/)
 [^4]: [aquasec](https://www.aquasec.com/blog/teamtnt-reemerged-with-new-aggressive-cloud-campaign/)
